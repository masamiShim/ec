<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="freitech.se.ec.repository.read.ItemReadRepository">
    <select id="findByPk" resultType="freitech.se.ec.mo.Item">
        SELECT
            I.id as id,
            I.exhibitor_id as exhibitorId,
            I.name as name,
            I.code as code,
            I.price as price,
            I.quantity as quantity,
            I.created as created,
            I.createdBy as createdBy,
            I.modified as modified,
            I.modifiedBy as modifiedBy
          FROM
            Item I
          WHERE
              Id = #{id}
    </select>
</mapper>
